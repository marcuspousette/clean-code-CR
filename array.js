const oldEmployees = ["Mike", "Nabil", "Cubby", "Mendes"];
const newEmployees = ["Marcus", "Rasmus", "Matt"];
const allEmployees = [];

oldEmployees.forEach((employee) => {
  allEmployees.push(employee);
});

newEmployees.forEach((employee) => {
  allEmployees.push(employee);
});
